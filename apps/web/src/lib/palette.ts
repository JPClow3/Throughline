/**
 * Project colour swatches, restricted to the Inkline signal palette
 * (docs/ui-ux.md §2). Values mirror the light-theme tokens in styles.css so
 * stored colours stay stable across themes.
 */
export const PROJECT_COLORS: readonly string[] = ["#3d5afe", "#1fae67", "#ffd43b", "#ff5d47", "#8f6bf5"];
